apiVersion: apps/v1
kind: Deployment
metadata:
  name: counter
  labels:
    io.kompose.service: counter
spec:
  replicas: {{ .Values.counter.replicas }}
  selector:
    matchLabels:
      io.kompose.service: counter
  template:
    metadata:
      labels:
        io.kompose.service: counter
    spec:
      containers:
        - name: counter
          image: {{ .Values.counter.image }}
          resources:
            requests:
              cpu: {{ .Values.counter.resources.requests.cpu }}
              memory: {{ .Values.counter.resources.requests.memory }}
            limits:
              cpu: {{ .Values.counter.resources.limits.cpu }}
              memory: {{ .Values.counter.resources.limits.memory }}
          ports:
            - containerPort: {{ .Values.counter.ports.containerPort }}
          envFrom:
            - configMapRef:
                name: counter-config
            - secretRef:
                name: postgres-db-creds
      restartPolicy: {{ .Values.counter.restartPolicy }}
