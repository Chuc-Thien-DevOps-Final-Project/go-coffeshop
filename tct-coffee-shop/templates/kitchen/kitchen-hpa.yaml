apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kitchen-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kitchen
  minReplicas: {{ .Values.kitchen.hpa.minReplicas }}
  maxReplicas: {{ .Values.kitchen.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: {{ .Values.kitchen.hpa.metrics.resource.name }}
        target:
          type: {{ .Values.kitchen.hpa.metrics.resource.target.type }}
          averageUtilization: {{ .Values.kitchen.hpa.metrics.resource.target.averageUtilization }}
